name: smart_meal
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  flutter_bloc: ^9.1.1
  equatable: any

  #connect to api
  http: ^1.4.0

  shared_preferences: ^2.5.3

  #Định dạng ngày giờ
  #intl: ^0.19.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
#  flutter_gen: ^5.3.2
flutter:
  uses-material-design: true

  generate: true

  assets:
    # Load toàn bộ thư mục images
    - assets/images/
    - assets/icons/

    # Hoặc load từng file cụ thể
    #- assets/images/register_bg.jpg
    #- assets/images/login_bg.png
    #- assets/images/logo.png

    # Load thư mục con
    #- assets/images/icons/

    # Load các file khác
    #- assets/data/